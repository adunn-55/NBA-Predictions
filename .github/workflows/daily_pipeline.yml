name: Daily NBA Prediction Pipeline

on:
  schedule:
    # Runs every day at 20:00 UTC (4:00 PM EST)
    - cron: '0 20 * * *'
  workflow_dispatch: # Gives you the manual button in GitHub UI

jobs:
  run-pipeline:
    runs-on: ubuntu-latest
